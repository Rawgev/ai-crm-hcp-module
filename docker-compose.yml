services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: ai_crm
      POSTGRES_USER: crm_user
      POSTGRES_PASSWORD: crm_password
    ports:
      - "5432:5432"
    volumes:
      - ai_crm_pgdata:/var/lib/postgresql/data

volumes:
  ai_crm_pgdata:
